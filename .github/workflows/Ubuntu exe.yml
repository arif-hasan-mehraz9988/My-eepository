name: Ubuntu RDP (User mehraz) - Fixed

on:
  workflow_dispatch:
    inputs:
      crd_code:
        description: 'Paste the Debian Linux code (4/0A...)'
        required: true
      pin:
        description: 'Set a 6-digit PIN'
        required: true
        default: '990088'

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
    - name: 1. Install Desktop & Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y --no-install-recommends \
          task-xfce-desktop xfce4 xfce4-goodies \
          curl wget xvfb python3-psutil

    - name: 2. Create User 'mehraz'
      run: |
        # Create user with home directory
        sudo useradd -m -s /bin/bash mehraz
        
        # Set password to 990088
        echo "mehraz:990088" | sudo chpasswd
        
        # Give sudo access
        sudo usermod -aG sudo mehraz
        echo "mehraz ALL=(ALL) NOPASSWD:ALL" | sudo tee /etc/sudoers.d/mehraz

    - name: 3. Install Google Chrome & Remote Desktop
      run: |
        # Download and install Chrome
        wget -q https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
        sudo dpkg -i google-chrome-stable_current_amd64.deb || sudo apt-get install --fix-broken -y
        rm google-chrome-stable_current_amd64.deb
        
        # Download and install CRD
        wget -q https://dl.google.com/linux/direct/chrome-remote-desktop_current_amd64.deb
        sudo dpkg -i chrome-remote-desktop_current_amd64.deb || sudo apt-get install --fix-broken -y
        rm chrome-remote-desktop_current_amd64.deb

    - name: 4. Configure Session (Fixed Step)
      run: |
        # Force stop any conflicting display managers
        sudo systemctl disable gdm3 2>/dev/null || true
        sudo service gdm3 stop 2>/dev/null || true
        sudo systemctl disable lightdm 2>/dev/null || true
        sudo service lightdm stop 2>/dev/null || true

        # Ensure the group exists before adding the user
        sudo groupadd -f chrome-remote-desktop
        sudo usermod -aG chrome-remote-desktop mehraz

        # Create the session configuration for XFCE
        # We use 'sudo tee' to write as root, then fix ownership
        echo "exec /etc/X11/Xsession /usr/bin/xfce4-session" | sudo tee /home/mehraz/.chrome-remote-desktop-session
        
        # Create config directory
        sudo mkdir -p /home/mehraz/.config/chrome-remote-desktop
        
        # Fix permissions: Give everything to mehraz
        sudo chown -R mehraz:mehraz /home/mehraz
        sudo chmod 700 /home/mehraz/.config/chrome-remote-desktop

    - name: 5. Start Chrome Remote Desktop
      env:
        DISPLAY_CODE: ${{ github.event.inputs.crd_code }}
        PIN: ${{ github.event.inputs.pin }}
      run: |
        # Pass environment variables explicitly into the sudo command
        sudo -u mehraz bash -c 'DISPLAY= /opt/google/chrome-remote-desktop/start-host \
          --code="'"$DISPLAY_CODE"'" \
          --redirect-url="https://remotedesktop.google.com/_/oauthredirect" \
          --name="Mehraz-Desktop" \
          --pin="'"$PIN"'"'

    - name: 6. Keep Alive Loop
      run: |
        echo "=========================================="
        echo "Session Started Successfully"
        echo "User: mehraz"
        echo "Password: 990088"
        echo "Go to: https://remotedesktop.google.com/access"
        echo "=========================================="
        while true; do sleep 60; done
